<!--Form to create a new item-->
<%= form_for(@photo_item) do |f| %>
  <% if @photo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_item.errors.count, "error") %> prohibited this photo_item from being saved:</h2>

      <ul>
      <% @photo_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<!--Form fields to create item goes here-->
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
